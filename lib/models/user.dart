
class User {
  String userName = 'Ahmed';
  String userType = 'parent';
  int userBackgroundColor;
  int userTextColor;

  User({this.userName, this.userType, this.userBackgroundColor, this.userTextColor});

  //String get backgroundColor => this.userBackgroundColor;
}
